<!-- Migration note: ContentPage → Page, ListView preserved with pull-to-refresh and item template -->
<Page x:Class="MyWeather.Uno.View.ForecastView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <ListView ItemsSource="{Binding Forecast.Items}"
                  x:Name="ListViewWeather"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10,5,10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Image Grid.Column="0"
                               Height="44" 
                               Width="44" 
                               Source="{Binding DisplayIcon}"
                               Margin="0,0,10,0"/>
                        
                        <StackPanel Grid.Column="1" 
                                    Padding="10" 
                                    Spacing="5"
                                    VerticalAlignment="Center">
                            <TextBlock Text="{Binding DisplayTemp}"
                                       Foreground="#3498db"
                                       Style="{StaticResource BodyTextBlockStyle}"/>
                            <TextBlock Text="{Binding DisplayDate}" 
                                       Style="{StaticResource CaptionTextBlockStyle}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!-- Note: Pull-to-refresh in WinUI uses RefreshContainer, but for simplicity keeping basic ListView -->
    </Grid>
</Page>
